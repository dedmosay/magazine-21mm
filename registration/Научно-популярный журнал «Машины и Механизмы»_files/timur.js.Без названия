$(document).ready(function(){

    /*$("body").on("ifChanged", ".ui_change_status", function(){
        if($(this).prop( "checked")){
            $("#"+$(this).attr("for")).selectmenu( "enable" );
        }else{
            $("#"+$(this).attr("for")).selectmenu( "disable" );
        }
    });*/
});